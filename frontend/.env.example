# API Base URL
# For local development leave empty to use the Vite proxy (http://localhost:5173 -> http://localhost:3001)
# For production (Vercel) set to your Railway backend URL including the /api prefix, for example:
# VITE_API_BASE_URL=https://ortam-docs-backend-production.up.railway.app/api
VITE_API_BASE_URL=
